spring:
  application:
    name: config-single-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://github.com/redelasi/config-repo #配置文件所在仓库
          username: redelasi
          password: Mingxing900926
          default-label: master #配置文件分支
          search-paths: /  #配置文件所在根目录
